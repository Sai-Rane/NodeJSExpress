const fs = require("fs"); // fs here stands for File System
console.log("fs", fs);

const hello = "hello world";
console.log(hello);
// In normal Javascript in the browser we use to include this js file in html file and then use to run the html file in the browser to see the output, but now with node we dont need to do anything like that and all we need to do is to use our node command in the terminal as 'node file_name'
